<h1>Events#index</h1>
<p>Find me in app/views/events/index.html.erb</p>



<div class="welcome">
  <div class='text-center'>
    <h1 class='mt-3 '>Bienvenue <%=@name%> sur EventBrite</h1><br>
    <a type="button" href="#" class="btn btn-primary btn-lg">Créer ton Events</a>
    <p class="mt-4">la liste de nos events</p>
  </div>

  <div class="row m-3">
    <% @event.all.each do |event|%>
            <div class="col-sm-4 mt-3">
                <div class="card">
                    <div class="card-body text-center mx-auto">
                        <h5 class="card-title"> Titre : <%=event.title%> </h5>
                        <a href= "/events/<%=event.id%>" class="btn btn-outline-primary btn-sm">En savoi plus</a>
                    </div>
                </div>
            </div>
    <%end%>
</div>
